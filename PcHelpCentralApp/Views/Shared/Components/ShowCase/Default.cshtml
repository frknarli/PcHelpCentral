@model IEnumerable<Solution>

<div class="text-center">
    <h1 style="font-family: 'Poppins', sans-serif; color: #ffffff;">Trends</h1>
</div>

<div class="row">
    @foreach (var prd in Model)
    {
        <partial name="_ProductCard" model="prd" />
    }
</div>
